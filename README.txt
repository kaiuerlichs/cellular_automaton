To compile this program, simply run the "make" command while in this folder.
Alternatively, use "g++ -Wall -Wextra -Werror -g Menu.cpp Menu.h Automaton.cpp Automaton.h helper.cpp helper.h -o AutomatonSimulation.out".

The program's usage should be self-explanatory.